function characterSequence(input){
    let text = input[0]
    //let buff = ""

    for (let i = 0; i < text.length; i++){
        console.log(text[i])
    }
    // buff += text[i] + ""  (ако искам да е на един ред)
}

characterSequence(["SoftUni"])